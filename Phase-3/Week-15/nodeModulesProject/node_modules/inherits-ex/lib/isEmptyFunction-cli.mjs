// Convenience re-export — identical to the canonical implementation.
// Kept as a separate file for backward compatibility with imports
// of `isEmptyFunction-cli` and the `isEmptyFunctionCli` named export.
export { isEmptyFunction, default } from './isEmptyFunction.mjs';
export { default as isEmptyFunctionCli } from './isEmptyFunction.mjs';
