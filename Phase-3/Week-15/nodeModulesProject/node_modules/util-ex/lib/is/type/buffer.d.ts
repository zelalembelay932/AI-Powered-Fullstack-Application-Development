/**
 *   Check if a value is a Buffer object.
 *   @param {*} arg - The value to check.
 *   @returns {boolean} - Returns `true` if `arg` is a Buffer object, else `false`.
 */
export const isBuffer: any;
export default isBuffer;
