"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
exports.isRegExp = isRegExp;
var _objectToString = _interopRequireDefault(require("../../object-to-string.js"));
var _object = _interopRequireDefault(require("./object.js"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
const regexpClass = '[object RegExp]';
/**
 * Checks if the given value is a RegExp instance
 * This function specifically determines whether the provided value is an actual RegExp object instance,
 * not just any object that might resemble a regular expression
 * @param {*} v - The value to check
 * @returns {boolean} Returns true if v is a RegExp instance, false otherwise
 *
 * @example
 * // returns true
 * isRegExp(/abc/);
 *
 * @example
 * // returns true
 * isRegExp(new RegExp('abc'));
 *
 * @example
 * // returns false
 * isRegExp('abc');
 *
 * @example
 * // returns false
 * isRegExp({});
 *
 * @example
 * // returns false
 * isRegExp(null);
 */
function isRegExp(v) {
  return (0, _object.default)(v) && (0, _objectToString.default)(v) === regexpClass;
}
;
var _default = exports.default = isRegExp;